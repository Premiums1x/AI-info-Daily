# AI Daily 项目现状总结

> 更新日期：2026-08-04  
> 当前阶段：可运行原型 + FastAPI/React 联调阶段  
> 项目性质：独立的 AI 每日资讯项目，不属于现有的 blogWeb

## 1. 项目定位

AI Daily 的目标是把过去 24 小时内值得关注的 AI 资讯整理成一份可快速阅读的日报。

当前产品方向已经从“后台管理式的资讯列表”转向“有阅读节奏的日报牌库”：

- 首页先展示一手精选资讯，降低第一次阅读的负担。
- 需要查看更多时进入完整牌库，并使用分页浏览，避免文章过多造成页面卡顿和溢出。
- 主题、信源和抓取状态由后端数据驱动，不再依赖前端写死的主题列表。
- Web 与未来的小程序等客户端共用同一套 JSON API，不为不同客户端复制后端业务逻辑。

## 2. 当前整体架构

```text
RSS / Atom 信源
       |
       v
httpx 抓取 -> feedparser 解析 XML -> 清洗/标准化 URL -> 去重
       -> 分类/标签提取 -> SQLite 持久化
                                      |
                                      v
                              FastAPI JSON API
                              /       |       \
                             /        |        \
                         React Web   未来小程序   其他客户端
```

主要目录：

| 目录 | 作用 |
| --- | --- |
| `backend/` | FastAPI 服务、RSS/Atom 抓取、解析、去重、分类、定时任务和 API |
| `Web/` | React + Vite Web 原型 |
| `mini-program/` | 为后续小程序客户端预留的目录，目前尚未完成完整接入 |
| `data/news.db` | 当前默认 SQLite 数据库 |
| `docs/` | 项目相关文档和设计资料 |

## 3. 已完成内容

### 3.1 后端能力

- 已建立独立 FastAPI 后端，不依赖 blogWeb。
- 已实现 RSS/Atom 抓取流程：
  - 使用 `httpx` 请求公开 Feed。
  - 使用 `feedparser` 解析 RSS/Atom XML。
  - 提取标题、原文链接、摘要、发布时间等字段。
  - 清理 HTML 标签和多余空白。
  - 规范化链接并移除常见追踪参数。
  - 通过 `canonical_url` 做唯一去重。
  - 按文章内容进行基础分类和标签提取。
- 已实现 SQLite 持久化：
  - `articles` 保存资讯内容和分类结果。
  - `sources` 保存信源配置及最近抓取状态。
  - 文章与信源通过 `source_code` 关联。
- 已实现信源注册机制：
  - 内置 `Google News · AI` 和 `Hugging Face Blog` 两个默认信源。
  - 默认信源缺失时会写入数据库，但不会覆盖用户已经修改的配置。
  - 用户新增的 RSS/Atom 信源会保存到数据库。
  - 新增信源默认启用，后续手动抓取或定时抓取会自动读取。
  - 单个信源失败不会阻断其他信源，错误会记录到 `last_error`。
- 已实现动态主题接口：
  - 根据指定时间范围内已解析文章的标签聚合主题。
  - 没有标签时使用有意义的文章分类兜底。
  - 不把“其他”生成成可展示主题。
- 已实现定时抓取：
  - 默认服务启动时立即执行一次抓取。
  - 默认每 120 分钟执行一次抓取。
  - 使用 APScheduler，限制同一任务同时只运行一个实例。
- 已实现健康检查，能够返回：
  - 数据库状态。
  - 调度器是否启用、上次运行时间、下次运行时间和最近状态。
  - 信源总数、健康数量和失败数量。
- 已实现手动触发抓取接口，并返回本次抓取的成功、失败、新增、更新和去重统计。
- 已提供 Docker Compose 运行方式。

### 3.2 Web 前端能力

- 已从静态原型迁移为 React + Vite 组件化实现，为后续继续拆分组件预留结构。
- 已完成 AI 日报主题视觉方向：
  - 温和的纸张/墨绿色/暖色点缀。
  - 毛玻璃表面和半透明卡片。
  - 避免传统后台管理页面的密集表格和蓝紫渐变。
- 已实现首页精选“手牌”：
  - 默认最多展示 7 张精选资讯牌。
  - 牌面支持发牌/翻牌式进入动效。
  - 第一次点击牌面只选中，第二次点击同一张牌才打开详情；点击空白、详情遮罩或按 Escape 键会取消选中并关闭详情。
  - 鼠标悬停或键盘聚焦时只提升当前牌清晰度，其他牌轻微降低饱和度；不移动牌面。
  - 文章数量较少时会根据数量优化牌的居中和平衡排列。
  - Hero 区域单独请求 `/daily-draw` 获取当天一张牌；后端不可用时回退到原型内置的第一张牌。
  - 下方精选区请求 `/featured` 获取最多 7 张牌；后端今日精选先分散信源，再补齐主题，最后按时间补足，规则可解释且不依赖 AI 推荐。
- 已实现文章详情层：
  - 点击资讯牌打开详情面板。
  - 支持关闭按钮、`Escape` 关闭和原文链接。
  - 详情面板使用主题化滚动条，不直接依赖浏览器默认视觉。
- 已实现搜索：
  - 搜索面板具有进出场动画。
  - 支持键盘快捷键 `Ctrl/Cmd + K`。
  - 支持按标题、摘要等已加载内容筛选资讯。
- 已实现主题和信源筛选：
  - 主题来自后端 `/topics` 接口。
  - 信源来自后端 `/sources` 接口。
  - API 不可用时保留静态数据回退，方便原型继续预览。
- 已实现完整牌库：
  - 精选区与完整牌库分离。
  - 完整牌库按 12 张一页静态切换，避免一次渲染大量牌造成卡顿。
  - 翻页后定位到今日牌库标题处，读者可以从当前页顶部继续向下浏览。
  - 牌库打开时保留当前页面位置，不强制用户跳到页面底部。
  - 提供固定的“返回精选”入口，查看大量资讯时可以随时返回。
- 已实现下滑后的固定导航：
  - 顶部导航超过视口后拆分为左侧日报信息和右侧操作按钮。
  - 右侧提供搜索、回顶部以及在适用状态下返回精选等操作。
- 已实现底部信源星图：
  - 信源以轨道/节点方式展示在日报结尾。
  - 节点数量和状态来自真实信源数据。
  - 支持从底部打开信源管理入口。
- 已实现前端信源管理：
  - 用户可以填写名称和公开 RSS/Atom 地址新增信源。
  - 可以启用或停用已保存信源。
  - 操作通过后端 API 完成，数据会持久化到 SQLite，而不是只保存在浏览器内存中。
- 已将底部“收到明日信号”按钮改为“抓取最新信号”，用于手动触发当前启用信源的即时抓取并刷新前端数据。

### 3.3 前后端联调

- 前端 API 基地址默认是：`http://127.0.0.1:8000/api/v1`。
- 支持通过 `VITE_API_BASE_URL` 覆盖 API 地址。
- 前端已联调以下能力：
  - 文章列表。
  - 动态主题。
  - 信源列表及状态。
  - 手动抓取。
  - 新增信源。
  - 启用/停用信源。
- 后端 CORS 已允许本地 Vite 常用端口 `5173` 和 `4173`。

## 4. 当前接口概览

| 方法 | 接口 | 用途 |
| --- | --- | --- |
| `GET` | `/api/v1/health` | 健康检查、调度器和信源状态 |
| `GET` | `/api/v1/articles` | 查询资讯，支持 `q`、`category`、`tag`、`since_hours`、`limit`、`offset`、`sort` |
| `GET` | `/api/v1/articles/{id}` | 获取单篇资讯 |
| `GET` | `/api/v1/categories` | 获取分类 |
| `GET` | `/api/v1/topics` | 根据最近抓取文章聚合动态主题 |
| `GET` | `/api/v1/daily-draw` | Hero 当天抽一张牌 |
| `GET` | `/api/v1/featured` | 获取下方精选牌（按信源和主题分散） |
| `GET` | `/api/v1/sources` | 获取信源目录、最近文章数和抓取状态 |
| `POST` | `/api/v1/sources` | 新增 RSS/Atom 信源 |
| `PATCH` | `/api/v1/sources/{code}` | 修改信源名称、地址或启停状态 |
| `POST` | `/api/v1/ingestion/refresh` | 手动抓取当前启用的信源 |

## 5. 数据和运行机制

### 5.1 数据保存边界

当前后端保存的是资讯元数据和 RSS 摘要：

- 标题。
- 来源。
- 发布时间。
- RSS/Atom 摘要。
- 分类和标签。
- 原文链接及规范化链接。

当前不会主动抓取和保存商业网站的完整正文，也还没有 AI 二次摘要、文章收藏、阅读记录等产品层能力。这是当前架构有意保留的范围边界，后续可以单独扩展。

### 5.2 抓取周期

默认配置如下：

```text
服务启动：立即抓取一次
定时任务：每 120 分钟抓取一次
单次 Feed 超时：20 秒
单个 Feed 最多处理：50 条
```

“后端自动抓取”目前已经存在；“前端自动更新”目前还没有实现，前端启动时会加载一次数据，之后主要依赖用户点击“抓取最新信号”手动刷新。

## 6. 当前验证结果

以下结果是在本次总结前重新核对过的：

### 后端

```text
python -m pytest -q
24 passed, 2 warnings

python -m compileall -q app
exit code 0
```

### 前端

```text
npm test
21 passed, 0 failed

npm run build
Vite production build succeeded
```

当前警告不影响上述结果，但建议后续处理：

- npm 配置中存在 `sass_binary_site` 未识别警告。
- Python 测试环境存在 Starlette/httpx 和 pytest-asyncio 相关弃用警告。

这些测试主要覆盖后端接口、前端模型和数据流，尚不能替代真实外部 RSS 网络环境、不同浏览器和移动端的完整端到端验证。

## 7. 当前已知问题与可改进项

下面按优先级排列，优先级表示对稳定性和实际使用体验的影响，不表示必须一次性全部完成。

### P0：建议优先处理

#### 1. 增加前端自动更新，但不要每次重新拉取全部文章

当前前端没有轮询。建议采用两段式策略：

1. 前端每 5～10 分钟请求一次轻量的健康/同步状态接口。
2. 只有检测到后端数据版本、最新采集时间或新增文章数量发生变化时，才重新请求文章、主题和信源。
3. 页面处于后台标签页时暂停轮询，重新获得焦点时补查一次。
4. 手动点击“抓取最新信号”后继续复用同一套刷新逻辑。

当前健康接口的 `last_run_at` 只能说明“抓取任务运行过”，不能准确说明“新增了文章”。更稳妥的做法是后端增加以下任一类标记：

- `data_version` / `content_version`。
- `latest_article_collected_at`。
- `new_articles_since_last_run`。
- 单独的 `/ingestion/status` 接口。

#### 2. 处理定时任务的多进程问题

当前 APScheduler 运行在 FastAPI 进程内部。单进程开发环境没有问题，但生产环境如果开启多个 Uvicorn/Gunicorn worker，可能出现多个进程重复抓取同一批信源。

生产化时应明确只保留一个调度进程，或者将调度器拆成独立 worker，并让 Web API 进程只负责接口请求。

#### 3. 完善信源可靠性和可观测性

用户现在可以自定义信源并保存到数据库，但真实 RSS 地址存在格式不规范、频繁超时、反爬、重定向和更新频率不稳定等情况。建议增加：

- 新增前的“测试连接”或 Feed 校验。
- 请求重试、指数退避和更细的超时配置。
- `ETag` / `Last-Modified` 条件请求，减少重复下载。
- 每个信源的最近成功时间、最近失败原因和连续失败次数。
- 编辑、停用、删除、重新测试单个信源的完整操作闭环。
- 信源级抓取耗时和新增条数统计。

### P1：稳定性和产品质量

#### 4. 提升资讯质量

当前解析的是 RSS/Atom 提供的摘要，分类和标签属于基础规则处理。后续可以逐步增加：

- 来源级解析规则，处理不同站点的字段差异。
- 更可靠的分类和标签词典。
- 资讯相关性评分，减少低价值聚合内容。
- 标题相似度/语义去重，识别不同信源对同一事件的重复报道。
- 可选的 AI 摘要、关键事实和“为什么值得关注”字段。
- 明确时区策略，统一展示时间并标注数据的新鲜度。

#### 5. 强化前端状态体验

当前视觉和核心交互已经成形，下一步建议补齐产品状态：

- 首屏加载骨架和抓取中的状态。
- 后端不可用、部分信源失败、无搜索结果、无主题和无文章的空状态。
- “数据更新时间”和“本次新增多少条”的提示。
- 抓取失败后的重试入口。
- 移动端小屏幕、触摸操作和横屏场景验证。
- 键盘焦点顺序、ARIA 标签、弹层焦点回收和减少动效设置的完整检查。
- 统一检查详情弹层、横向滚动区域在不同浏览器中的主题化滚动条表现。

#### 6. 继续收敛前端组件边界

当前已经有 `SignalArchive`、`SourceAtlas`、`SourceManager` 等组件，但 `App.jsx` 仍然承担较多页面状态和业务编排。后续可以按以下方向拆分：

- `useNewsData`：文章、主题、信源和刷新状态。
- `useSearch`：搜索面板、快捷键和查询状态。
- `useArchiveNavigation`：精选/牌库切换、分页和滚动定位。
- `DailyHeader`、`CardHand`、`FocusSheet`、`StickyDock` 等展示组件。
- API 层统一错误类型和响应转换。

拆分时应保持现有视觉和交互不变，先抽离状态边界，再考虑更大范围的重构。

### P2：生产化基础设施

#### 7. 数据库从 SQLite 平滑演进

SQLite 很适合当前单机原型和低并发场景。若后续需要多实例部署、后台管理、用户行为数据和更复杂查询，建议：

- 引入 Alembic 数据库迁移。
- 为文章、发布时间、来源和规范化 URL 建立/检查索引。
- 评估 PostgreSQL 迁移。
- 增加数据库备份、恢复和旧文章保留策略。
- 对全文搜索评估 SQLite FTS 或 PostgreSQL 全文检索。

#### 8. API 安全和权限

当前新增/修改信源以及手动抓取接口适合本地原型，但公开部署前需要：

- 管理接口鉴权。
- 手动抓取的频率限制。
- 信源地址校验，避免访问内网地址或非预期资源。
- 更明确的错误码和统一响应格式。
- 生产环境通过环境变量配置 CORS、数据库和抓取参数。
- 记录结构化日志，避免只依赖控制台文本。

#### 9. 部署和运行脚本

目前已经有本地命令和 Docker Compose，但还可以继续补齐：

- 一键启动前后端的 Windows 脚本。
- 开发、测试、生产配置分离。
- API、数据库和调度器健康检查。
- 反向代理、HTTPS、静态资源缓存和日志轮转。
- 固定 Python 依赖版本并补充 lock 文件/可复现安装说明。

### P3：产品扩展

这些能力不影响当前原型，但可以作为后续产品路线：

- 用户收藏、稍后阅读和已读状态。
- 按日期查看历史日报。
- 用户关注主题和信源。
- AI 每日摘要、事件时间线和跨信源聚合。
- 通知推送或邮件日报。
- 小程序客户端复用现有 API。
- 多语言和更多地区化信源。

## 8. 当前推荐的下一步路线

建议按照下面顺序推进：

### 第一阶段：把“新鲜度”闭环做完整

- [ ] 后端增加数据版本或最新文章标记。
- [ ] 前端增加 5～10 分钟轻量轮询。
- [ ] 仅在检测到数据变化时刷新文章、主题和信源。
- [ ] 增加页面隐藏/重新聚焦处理。
- [ ] 在界面上显示最近更新时间和抓取结果。

### 第二阶段：把信源能力做稳定

- [ ] 增加测试 Feed 接口。
- [ ] 增加重试、退避和条件请求。
- [ ] 增加信源编辑、删除、重试和连续失败提示。
- [ ] 增加信源抓取统计和结构化日志。

### 第三阶段：把运行环境做成生产可用

- [ ] 将调度器与 API 进程解耦，避免多 worker 重复抓取。
- [ ] 增加鉴权、限流和 SSRF 防护。
- [ ] 增加 Alembic 迁移、备份和 PostgreSQL 评估。
- [ ] 完善 Docker/Windows 一键启动和部署文档。

### 第四阶段：增强日报阅读价值

- [ ] 增加 AI 摘要和事件聚合。
- [ ] 增加收藏、已读和历史日报。
- [ ] 接入小程序或其他客户端。

## 9. 本地启动方式

### 启动后端

在 `backend` 目录执行：

```powershell
python -m pip install -r requirements.txt
Copy-Item .env.example .env
python -m uvicorn app.main:app --reload --port 8000
```

后端启动后：

- Swagger：<http://127.0.0.1:8000/docs>
- 健康检查：<http://127.0.0.1:8000/api/v1/health>
- 文章接口：<http://127.0.0.1:8000/api/v1/articles>

如需只执行一次抓取，可以执行：

```powershell
python -m app.jobs.run_ingest
```

### 启动前端

在 `Web` 目录执行：

```powershell
npm install
npm run dev
```

或者使用项目中固定 `4173` 端口的脚本：

```powershell
npm run react
```

前端默认地址：<http://127.0.0.1:4173/>

## 10. 重要说明

- 当前工作区存在 Web、backend 和数据库相关的未提交修改。本文件是阶段性总结，不代表这些改动已经拆分提交或完成发布审核。
- 当前项目已经具备“抓取—保存—API—展示—手动刷新—信源管理”的最小闭环，但还不是生产级资讯平台。
- 当前最值得优先完成的不是继续增加视觉效果，而是补上前端自动感知更新、抓取状态可观测性和多进程调度安全。
- 不同客户端不建议维护不同版本的后端。推荐继续使用同一套领域服务和 JSON API，让 Web、小程序等客户端只在展示层和交互层做差异化。

## 11. 关键参考文件

- 后端接口和运行说明：`backend/README.md`
- 后端架构说明：`backend/ARCHITECTURE.md`
- 后端配置：`backend/app/core/config.py`
- 后端应用入口：`backend/app/main.py`
- 抓取解析：`backend/app/ingestion/fetcher.py`、`backend/app/ingestion/parser.py`、`backend/app/ingestion/pipeline.py`
- Web 页面入口：`Web/src/App.jsx`
- Web API 层：`Web/src/api/newsApi.js`
- 完整牌库：`Web/src/components/SignalArchive.jsx`
- 信源星图：`Web/src/components/SourceAtlas.jsx`
- 信源管理：`Web/src/components/SourceManager.jsx`
- Web 样式：`Web/styles.css`

